{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build All C Files",
            "type": "shell",
            "command": "gcc Code/*.c -I HeadFiles -o test.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}